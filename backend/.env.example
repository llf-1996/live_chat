# ================================
# 在线客服系统 - 后端环境变量配置
# ================================
# ⚠️ 所有配置项都是必需的，必须显式设置，没有默认值！
# ⚠️ 配置缺失时启动会抛出 ValueError 异常！
# ⚠️ 使用 'is None' 判断配置是否存在（不用 'if not value'）
# ================================

# ==================== 数据库配置 ====================

# 数据库连接地址（必需）
# 格式：mysql+aiomysql://用户名:密码@主机:端口/数据库名
# 示例：mysql+aiomysql://root:123456@localhost:3306/chat
DATABASE_URL=mysql+aiomysql://username:password@host:11003/database

# SQL 调试模式（必需）
# True: 在控制台打印所有 SQL 查询语句
# False: 不打印 SQL 语句
# ⚠️ 生产环境必须为 False
DEBUG_SQL=False


# ==================== JWT 认证配置 ====================

# JWT 密钥（必需）
# ⚠️ 生产环境必须使用强密钥
# 生成：python -c \"import secrets; print(secrets.token_urlsafe(64))\"
JWT_SECRET_KEY=your-secret-key-here-please-change-in-production

# JWT 加密算法（必需）
JWT_ALGORITHM=HS256

# JWT Token 有效期（天）（必需）
JWT_ACCESS_TOKEN_EXPIRE_DAYS=7


# ==================== 服务器配置 ====================

# 监听地址（必需）
# 0.0.0.0: 监听所有网卡  127.0.0.1: 仅本地
HOST=0.0.0.0

# 监听端口（必需）
PORT=11075

# 热重载模式（必需）
# True: 代码修改后自动重载（开发环境）
# False: 不自动重载（生产环境）
# ⚠️ 生产环境必须为 False
RELOAD=False

# 调试模式（必需）
# True: 返回详细错误堆栈（开发环境）
# False: 仅返回通用错误信息（生产环境）
# ⚠️ 生产环境必须为 False
DEBUG=False


# ==================== 应用配置 ====================

# 基础 URL（必需）
# 用于拼接静态资源完整 URL
# 开发：http://localhost:11075  生产：https://api.yourdomain.com
BASE_URL=http://localhost:11075

# CORS 跨域配置（必需）
# 多个地址用逗号分隔，无空格
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# 媒体文件目录（必需）
MEDIA_DIR=media

# 文件大小限制（字节）（必需）
# 10485760=10MB  52428800=50MB  104857600=100MB
MAX_FILE_SIZE=10485760


# ==================== 应用元信息 ====================

# 应用标题（必需）
APP_TITLE=在线客服系统

# 应用描述（必需）
APP_DESCRIPTION=基于FastAPI和WebSocket的实时在线客服系统

# 应用版本（必需）
APP_VERSION=1.0.0


# ================================
# 重要提示
# ================================
# 1. ⚠️ 所有配置项都是必需的（16项）
#    配置缺失时启动抛出 ValueError 异常
#
# 2. 生产环境安全检查清单：
#    ✅ RELOAD=False（必须）
#    ✅ DEBUG=False（必须）
#    ✅ DEBUG_SQL=False（必须）
#    ✅ JWT_SECRET_KEY 使用强随机密钥（必须）
#    ✅ CORS_ORIGINS 设置为实际前端地址
#    ✅ BASE_URL 使用 HTTPS 协议
#
# 3. 配置验证使用 'is None' 判断：
#    value = os.getenv(\"KEY\")
#    if value is None:  # 正确
#        raise ValueError(\"未设置\")
#
# 4. 有效配置值包括：
#    \"False\", \"0\", \"\"（空字符串）都是有效值
#    只有 None（未设置）才会报错
# ================================
